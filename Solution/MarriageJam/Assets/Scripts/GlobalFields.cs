public enum MAP_TYPE
{
    WALL, GROUND
}

public enum TAGS
{
    Player,
    Enemy,
    DestroyableItem
}

public enum BUTTONS
{
    Attack
}

public enum GameState
{
    Wait,
    Play,
    Pause,
    Win
}

public enum Difficulty
{
    Easy,
    Normal,
    Hard
}